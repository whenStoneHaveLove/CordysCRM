# 2026-09-16

## 巨坑：ad_order_contract 保存合同报 Duplicate entry 'uk_ad_oc'
- 报错：`AdContractService.syncOrderContract(AdContractService.java:444)` 插 `ad_order_contract` 触发 `Duplicate entry 'orderId-contractId' for key 'ad_order_contract.uk_ad_oc'`。
- 根因：`uk_ad_oc (order_id, contract_id)` **不含 deleted**（DDL 见 `V3.0.0_1__ad_system_v31.sql:322`），而合同端 `syncOrderContract` 是「先逻辑删除旧关联再 insert」。逻辑删除的行仍占用唯一键 → 第二次保存同一对关联必炸；`orderIds` 有重复项时也会炸。
- 修复（后端）：
  1. `ExtAdOrderContractMapper` 新增 `selectAllByContractId(contractId)`（`@Select ... WHERE contract_id = ?`，**不过滤 deleted**），与已有的 `selectAllByOrderId` 对称。
  2. `AdContractService.syncOrderContract` 改为「查全部（含已删）→ 不在目标集合的清 deleted=1 → 目标集合里已有的复用（deleted 复位为 0，update）→ 没有的才 insert」，`orderIds` 用 `LinkedHashSet` 去空去重。与 `AdOrderService.syncOrderContract` 的实现思路保持一致。
- **通用教训**：凡「逻辑删除 + 唯一键」的表，唯一键不含 `deleted` 时**不能先删再插**，必须复用旧行（顺带把 deleted 复位）。项目里同坑位：`ad_order_contract.uk_ad_oc`、`ad_order_downstream_media.uk_ad_odm`（订单端已处理）、`ad_payout_invoice.uk_payout_invoice_payout`（payout 端用 update 逻辑，且清除是物理删除，安全）。
- 未编译验证（Maven 离线缺 jacoco 依赖、在线编译用户不让等）；`read_lints` 无诊断。mvnw 在项目根目录，命令：`mvnw.cmd -q -pl crm -am -DskipTests -Djacoco.skip=true compile --file backend/pom.xml`。

## 付款单页（advertising/payout/index.vue）ESLint 报错修复
- 报错：`uploadTempAttachment` no-undef、`downloadInvoice` no-use-before-define、2 处 prettier 换行。
- 修复：
  1. 从 `@/api/modules` 补 import `uploadTempAttachment`（放 `updateAdPayout` 之后，保持 simple-import-sort 字母序）。
  2. `downloadInvoice` 定义移到 `previewInvoice` 之前（eslint 规则对函数声明也生效）。
  3. prettier：columns 的 `mediaNames` 列拆多行；`applyQuery` 里过长的 if 语句体换行。
- 关键约定：`uploadTempAttachment` 返回值是原生响应，取文件 ID 用 `res?.data?.[0] || res?.data`（参考 `contract/create.vue`、`crm-file-input/index.vue`），不能直接当 string 用。

## 付款单发票功能打通（详情/审批中心都看不到发票的根因）
- 根因：前端 `handleSave` 组装 payload 时**根本没带** `invoiceTempFileId/invoiceFileName/invoiceNo`，`openEdit` 也**没回填**已有发票 → 发票永远存不进库 → 详情只显示「未上传发票」，审批中心也没有该字段。
- 修复（前端 `payout/index.vue`）：
  1. `InvoiceFile` 增加 `isNew` 标记：**只有本次新上传**才把 fileId 当 tempFileId 传给后端转存；编辑回填的（已转存过的）不传，避免重复 processTemp。
  2. `handleSave`：删除 → `invoiceTempFileId='__CLEAR__'`；新上传 → 传 tempFileId+fileName；未变更 → 都不传；`invoiceNo` 恒定传。
  3. `openEdit` 回填 `form.invoiceNo` 与 `invoiceFile`；保存成功后 `isNew=false`、`invoiceCleared=false`。
  4. 表单新增「发票号」输入（`form.invoiceNo`，`resetForm` 同步清空）；详情抽屉发票区新增发票号 + 文件名。
- 修复（前端 `approval/index.vue`）：payout 详情 base 字段追加「发票号」和「发票」（`attachmentUrl`，复用已有预览/下载按钮渲染逻辑）。
- 修复（后端 `AdPayoutService.saveInvoice`）：原来 `tempFileId==null` 直接 return，导致**只改发票号不换文件时不生效**；改为 exist 非空且 `invoiceNo!=null` 时执行 `extAdPayoutInvoiceMapper.update(exist)`（BaseMapper 有 selective 的 `update(E)`）。
- 注意：`ExtAdPayoutInvoiceMapper` 是纯注解 mapper（无 XML），`selectByPayoutId` 走 `@Select`。

## 发票详情 UI 调整（去冗余）
- 付款单详情抽屉：原「发票号 / 文件」两行 + 下方重复的附件块 → 改为「发票号 / 发票」两项，**发票项 = 文件名 + 预览/下载按钮同行**（`flex items-center gap-8` + `max-w-[320px] truncate`），删除多余附件块（`<n-empty>未上传发票</n-empty>` 也一并去掉，无发票显示 `-`）。
- 审批中心 `approval/index.vue`：附件字段渲染由「只显示按钮」改为支持 `BaseField.showValue` 标记 —— `showValue: true` 时在按钮前显示 `f.value` 文本（付款单发票用）。
  注意 `detailBaseFields` 的 ref 泛型与 `BaseField` 类型是**两处分别声明**，加字段要同步改。
- 付款单 `ad_payout_invoice` 明细里的字段名：后端 response 是 `{ invoiceNo, fileName, fileUrl }`。

## 巨坑：附件预览图片变下载（Content-Type 判断漏了图片）
- 现象：付款单详情/审批中心点「预览」jpg 发票，浏览器直接下载。
- 根因：`AttachmentService.resolveContentType(fileName, fallback)` **只识别 svg 和 pdf**，图片会落到 fallback `application/octet-stream` → 浏览器强制下载。
  注意 `getResource` 有两条分支：`attachment == null`（temp 临时目录）分支用 `Files.probeContentType` 能出 image/jpeg；但 **`attachment != null`（已转存的正式附件）分支只走 resolveContentType**，所以已保存的发票预览必下载。
- 修复：`resolveContentType` 增加图片分支，按扩展名返回 `image/png|gif|webp|bmp|jpeg`；同时 `isSvg` 补 null 判断（原来 `fileName.endsWith` 无 null 保护）。
- 复用：以后任何"附件预览变下载"的问题先看 `AttachmentService.resolveContentType`；预览必须 image/* 或 application/pdf 才会 inline。
- 前端预览统一写法：`/attachment/preview/{fileId}?userId=xxx`（`seal/detail.vue`、`contract/detail.vue`、`order/detail.vue`、`approval/index.vue` 一致；userId 后端未实际使用，仅保持一致）。

## 巨坑：新建 ad_* 子表 DDL 列类型必须对齐 BaseModel（String/Long）
- 报错：`Incorrect integer value: 'admin' for column 'create_user'`（INSERT ad_payout_invoice 时）。
- 根因：`cn.cordys.common.domain.BaseModel` 的字段类型是 **`String id` / `String createUser` / `String updateUser` / `Long createTime` / `Long updateTime`**（注意：**id 是 String 不是 Long，时间戳是 BIGINT 毫秒不是 DATETIME**）。我上一版 DDL 按传统习惯写成了 `id BIGINT` / `create_user BIGINT` / `create_time DATETIME` → 插入字符串 id 与 'admin' 直接报 1366。
- 模板参考：`V3.0.0_13__ad_receipt_payment.sql`（ad_receipt/ad_payment 建表），标准写法 `id VARCHAR(32) NOT NULL`、`create_time BIGINT`、`create_user VARCHAR(32)`、`organization_id VARCHAR(32)`。
- **BaseMapper.InsertSqlProvider 是非选择性 INSERT**：`INTO_COLUMNS(table.getColumns())` 会插入实体所有字段，所以表必须有实体全部字段的列（多出来的列无妨）。
- 处理方式：
  1. 改 `V3.0.0_38__ad_payout_invoice.sql` 为正确类型（新库正确）。
  2. 新增 `V3.0.0_39__fix_ad_payout_invoice_columns.sql` 做幂等 `MODIFY COLUMN` 修复已建表的库。
- **本项目的 `spring.flyway.validate-on-migrate=false`**（`backend/app/src/main/resources/commons.properties`）→ **已执行过的迁移文件可以放心改内容**，不会因 checksum 变化启动失败；但改动不会重跑，所以要另加新版本号迁移去修数据/结构。
- 迁移版本号全局唯一（ddl/dml 共用一套版本序列，Flyway location 是 `classpath:migration` 一个根）。

## 订单/合同附件 PDF 预览支持
- **合同附件**（`AdContractAttachmentService`）走 `processTemp` 转存正式目录 + 写 `sys_attachment`（`fileUrl` 即 tempFileId 也是 attachment id）→ 预览走 `getResource` 的 `attachment != null` 分支；PDF 内联预览靠上一节 `resolveContentType` 的 `isPdf` 分支，已生效。
- **订单附件**（`AdOrderAttachmentService.upload`）**只调 `uploadTemp`，从不 `processTemp`** → 文件一直躺在 `tmp/` 目录，无 `sys_attachment` 记录 → 预览走 `attachment == null` 的临时目录分支（`DefaultRepositoryDir.getTempFileDir(tempFileId)`）。
  - 临时分支的 Content-Type 也走 `resolveContentType(file.getName(), ...)`，而 temp 保存时保留了原始文件名（`file.getOriginalFilename()`），所以扩展名判断同样生效 → PDF/图片可内联预览。
  - **隐患**：`TaskCleanupJob` 每天 3 点清理临时文件，订单附件长期会 404（预览/下载失效）。若要彻底解决需让 `AdOrderAttachmentService` 也调 `processTemp`。
- 前端改动：`order/create.vue` 三个附件上传位（type 10/20/40）原本只有 n-upload 默认文件列表、**没有预览入口**；现改为 `:show-file-list="false"` + 自渲染列表（`.attach-file-list`），每项带「预览/下载/删除」。
  1. 预览：新选文件（还在 `pendingFiles` 里的 `File`）用 `URL.createObjectURL` 本地 blob 打开；已保存附件走 `/attachment/preview/{fileUrl}`；非 PDF/图片（doc/xls 等）自动降级为下载。
  2. 删除：自定义按钮调 `handleAttachRemove(type, file)` → 内部先 `handleFileRemove`（返回 false 则不删）再按 `id` 从 `fileListTypeX` 过滤，并按原始 `File` 对象兜底清 `pendingFiles`。
  3. `handleFileSelect` 里原来的 `opts.file.id = _key` **改不到列表对象**（naive-ui `createSettledFileInfo` 会对 custom-request 收到的 file 做浅拷贝），所以补了一句按 `f.file === rawFile` 定位 `fileListTypeX` 里的真实项并回写 `id = _key`，否则暂存文件删不掉、仍会被上传。
  4. 查 naive-ui 内建 `file` 插槽不可靠（`es/upload` 里查不到 `slots.file`），改用项目已有的「自渲染列表」方案更稳。
- 其余页面（`order/detail.vue`、`contract/detail.vue`、`contract/create.vue`、`seal/detail.vue`）本来就有预览按钮，走 `/attachment/preview/{id}`，后端 Content-Type 修好后 PDF 已可直接内联预览，无需改动。

## 广告订单「复制」功能（已完成）
需求：订单列表「详情」列 → 「操作」，详情后加「复制」按钮（带权限，三处一致）；弹窗勾选复制字段（默认勾选：业务主体、订单类型、客户ID、上游代理、下游客户、返点方式、收款方式、账期天数）；点复制生成**草稿**订单，名称加 `-复制`，未勾选字段置空；放宽 ad_order 非空约束；前端校验保留。

后端：
- `PermissionConstants.AD_ORDER_COPY = "AD_ORDER:COPY"`；`permission.json` 加 `{"id":"AD_ORDER:COPY"}`；i18n 加 `permission.copy`（复制/Copy）。权限树由 `permission.json` 运行时读取（`getPermissionDefinitions()`），**不需要往 sys_permission 插数据**，只插 `sys_role_permission`。
- 新增 `AdOrderCopyRequest`（id/fields/orderName）、`AdOrderController.copy` = `POST /api/ad/order/copy`、`AdOrderService.copy`（`@Transactional` + `@OperationLog(module="AD_ORDER", action="COPY", targetId="")`）。
- 字段 key 常量内聚在 `AdOrderService.CopyField`（嵌套类），默认勾选集合 `DEFAULT_COPY_FIELDS`；前端 `copyFieldGroups` 的 key **必须**与之同名。
- copy 流程：requireOrder → 按 fields 逐列 if 赋值（未勾选天然为 null）→ `generateOrderNo` → `amountCalculator.computeAmounts` → 未勾选下游客户时强制把 `mediaPayableAmount/actualMediaPayableAmount/mediaRebateAmount` 置 0（否则 computeAmounts 会用总额兜底）→ insert → 勾选 contractId 才 `syncOrderContract` → 勾选 downstreamMedia 才 `syncOrderDownstreamMedia`（把源 `AdOrderDownstreamMedia` 转成 `AdOrderSaveRequest.DownstreamMediaPayableDTO` 复用现有同步逻辑）。
- **编号坑**：`countTodayOrders` 的 `business_entity_id = ?` 在字段为 NULL 时恒不成立 → 复制（不勾业务主体）时流水永远 001。新增 `ExtAdOrderMapper.countTodayOrdersWithoutEntity`（XML 里 `business_entity_id IS NULL`），`generateOrderNo` 按 id 是否为空二选一。
- 迁移：`ddl/V3.0.0_40__ad_order_copy_nullable.sql`（把 business_entity_id / order_type / customer_id / total_amount / rebate_mode / rebate_value / rebate_amount / receivable_amount / media_payable_amount / delivery_start_date / delivery_end_date / receipt_method MODIFY 成 NULL，**保留默认值**；no_rebate_amount 等自带 NOT NULL DEFAULT 0 不动）、`dml/V3.0.0_41__ad_order_copy_permission.sql`（给超管/含 AD_ORDER:CREATE 的角色授 AD_ORDER:COPY，仿 V3.0.0_37 写法）。
- 新增操作日志 action `COPY` → 已同步 `config/adLog.ts` + zh-CN.ts/en-US.ts 的 `advertising.log.action.COPY`（**加 action 必改这三处**）。

前端：
- `order/index.vue`：action 列 title 改 `t('advertising.order.action')`、width 150→210，新增复制按钮（`withDirectives(..., [[permissionDirective, ['AD_ORDER:COPY']]])`，任何状态都可点）；新增复制弹窗（`n-modal preset="card"` + `#footer` 手动按钮 + `n-checkbox-group`），字段按 6 个分组展示，带全选/清空/已选计数。
- `lib-shared`：`models/advertising.ts` 加 `AdOrderCopyParams`；`api/requrls/adOrder.ts` 加 `AdOrderCopyUrl`；`api/modules/adOrder.ts` 加 `copyAdOrder`；`web/src/api/modules/index.ts` 导出。
- i18n 新增键：`advertising.order.action/copy/copyTitle/copyTip/copySource/copySelectAll/copyClear/copySelectedCount/copyEmptyFields/copySuccess/copyCancel/copyConfirm/copyField.downstreamMedia/copyField.group.{base,amount,delivery,receipt,payment,other}`（zh-CN + en-US 同步）。
- 注意：i18n 插值参数不要用 `count`（vue-i18n 会当复数处理），本轮改成 `{num}`。
- 复制成功后刷新列表并回到第 1 页，**不自动跳转编辑页**。
- 修正：操作列 3 个按钮（详情/复制/编辑）在 width 210 下会换行 —— naive-ui `NSpace` 默认 `wrap: true`，且单元格左右内边距吃掉 24px。已把 `width` 改为 **260** 并给该列 `NSpace` 显式 `{ wrap: false }`。
- 另一个坑：`refreshColumns` 里锁定列的宽度是 `lockedSaved?.width ?? c.width`，**用户持久化的旧宽度会覆盖代码新值**，改宽不生效。已对 `key === 'action'` 特判强制用代码宽度（忽略历史持久化）。

## 会话：项目熟悉（读记忆 + 提交记录）
- 复核确认「复制订单」功能前后端均已落地并提交（`8550444fb`，18 文件）；MEMORY.md 里该条目原标"进行中/待办"已更正为已完成。
- 当前工作区仅剩 3 个 staged 文件（`AdOrderCopyRequest.java`、`V3.0.0_40__ad_order_copy_nullable.sql`、`V3.0.0_41__ad_order_copy_permission.sql`），无其他脏文件，无未 push 检查需求。
- Flyway 3.0.0 已用到版本 41（ddl 40 / dml 41），后续新增迁移需 >41。

## 订单列表右侧「空白列」（结论：是操作列本身过宽，非 scroll-x）
- 现象：订单列表最右侧看着像多出一列空白，占界面。
- 第一次判断错了：以为是 `scrollX = 可见列宽之和 + 100` 的余量造成，改掉 `+100` 后用户说不是想要的 → **已回滚**，`scrollX` 恢复原样（保持 +100）。
- 真因：action 列 width 260 太宽（多数订单只有「详情/复制」两个按钮，右侧大片留白像一列空白）。
- 最终改动：`order/index.vue` 的 action 列 `width: 260 → 190`（3 个小按钮约 150 + 间距 16 + 单元格内边距 24），`NSpace` 已有 `wrap: false` 保证不换行。
- 教训：用户说"这一列太宽"时，先确认是哪一列、是不是操作列；不要先动 `scroll-x` 这种影响全局布局的东西。

## 订单附件校验改造：新增 eml 邮件记录（60）
- 需求确认结果（用户三项均选推荐项）：①有 eml 就放行（排期+截图都可省），无 eml 则 10+20 都必传；②创建页附件区改两个 tab 切换；③草稿详情页也要能传 eml。
- 后端：新增 `AdAttachmentType` 枚举（ad/common/constants，风格照 OrderType）；`AdOrderService.submit` 改为"先判 hasAttachment(60)，无则 require(10)+require(20)"，新增私有 `hasAttachment`；`AdOrderAttachmentService.upload` 对 60 校验 `.eml`（新增 `EML_SUFFIX` + `isEml()`）；Domain/Controller 注释补 60。
- 前端：create.vue 加 `attachTab`/`fileListType60`/`hasEmailRecord`/`scheduleLabel`/`emailScreenshotLabel`，`getAttachFileList` 与 `buildFileListForType(60)` 补齐，提交校验包在 `if (action === 'submit' && !hasEmailRecord.value)`；detail.vue 加 eml 上传按钮（accept=".eml"）+ attTypeLabel 60 + 空状态描述。
- 无 DB 迁移（附件类型是 int，前端/后端硬编码标签）。
- 验证：ESLint（两个 vue 文件）0 error 0 warning；Java 未跑编译（仅肉眼复核 + IDE 诊断 0）。

## 附件预览 401 排查（用户报 `/attachment/preview/{id}?userId=admin` 打开 401）
- 结论：**与本轮 eml 改动无关**（只动了 ad 订单附件相关 4 个 Java + 2 个 vue，未碰 security）。
- 链路：`ShiroFilter.addFileAccessFilters` 把 `/attachment/preview/**`、`/attachment/download/**`、`/pic/**` 挂 `authf` → `FileAccessAuthFilter` 只认 **Cookie `F_A_TOKEN`** → `FileAccessTokenUtils.getSessionId` 用 `cordys.secret.key`(注入到 `SessionUser.secret`) AES 解密 → `SessionUtils.sessionExists(sessionId)` 查 **Redis**（`RedisIndexedSessionRepository.findById`）。
- 401 三种根因：①没重新登录 / 无痕窗口直接粘 URL（无 cookie）；②Redis 重启或清库，session 不存在；③`cordys.secret.key` 变了，旧 cookie 解不开。
- 快速判断：F12 → Application → Cookies 里有没有 `F_A_TOKEN`；有的话基本就是 Redis session 没了或 secret 变了 → 重新登录即可。
- 注意：`13ac16a7c refactor: remove unused public path filters from ShiroFilter`（2026-07 上游提交）已把这些路径从 anon 移除，所以老版本能匿名预览、现在不行。
- 补充机制差异（解释"接口正常、只有预览 401"）：普通 `/api/**` 走 `apikey, csrf, authc`，`ApiKeyFilter` 支持 header token（`CommonBeanFactory.getUser`）自动 `subject.login`；而 `window.open` 打开预览**带不上自定义 header**，只能靠 cookie → 所以页面一切正常时预览照样可能 401。
- 过滤器链顺序：`ShiroFilterFactoryBean` 的 `filterChainDefinitionMap` 是 LinkedHashMap，`/attachment/preview/**`(authf) 在 `/**`(apikey,csrf,authc) 之前 → 预览确实走 authf，不走 authc。
- 其他确认点：vite dev proxy 有 `/attachment` 规则（rewrite 仅处理 `/front/attachment` 前缀，对预览路径不改写，原样转发）；`cordys.secret.key` 为固定值 `9a9rdqPlTqhpZzkq`；`spring.session.timeout=43200s`（12h，存 Redis）。
- 已逐行核对本次未提交 diff：只动「eml 上传后缀校验」「提交守卫 + hasAttachment」「UI 文案/按钮」，**不触碰预览链路**，确认非本次改动引入。
- 根治方案（未实施，待用户决定）：预览 URL 带短期 token 参数（后端签发、前端拼接），或本地临时把 `/attachment/**` 设 anon。

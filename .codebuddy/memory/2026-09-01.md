# 2026-09-01

## 补数公式最终版：直接累加明细 actual_payable 列
- 用户截图 `ad_order_downstream_media` 表，列 `actual_payable` 确实有值（10.00、10.90、80.00、null 等），说明 source of truth 就是这一列，不是 `payable_amount - rebate_amount`。
- 修正 `backend/crm/.../AdOrderService.java` 的 `applyDerivedOnlyFromOrder`：
  - 旧：遍历明细累加 `payableAmount - rebateAmount`（担心 actual_payable 历史为 0/null 用公式兜底）。
  - 新：直接 `sum(d.getActualPayable())`，跳过 null 行。
- 口径与下游「实际应付总额（各客户累加）」底部展示一致，与前端明细行 actualPayable 展示一致。
- 补数三列不变（实际应付 / 应付返点 / 订单收入），只改首个字段的取数来源。
- 提交：`086b85805`（fix(ad-order): 补数改为直接累加下游明细 actual_payable 列）
- 用户验证步骤：重启后端 → 管理员点「补算数据」→ 抽查订单主表 `actual_media_payable_amount` 应等于各下游明细 `actual_payable` 之和。

## 补数 fallback：actual_payable 为 null 时用应付-返点兜底
- 现场问题：订单 YYW-20260825-002 补完数后实际应付仍 = 0；前端明细行能展示 10.90，但底部「实际应付总额（各客户累加）」也 = 21.80，主表依然是 0。
- 根因：这条订单对应明细的 `ad_order_downstream_media.actual_payable` 库列**是 null**（同库其他行有值）；前端明细行/底部累加其实是前端按 `payableAmount - rebateAmount` 实时算的，并没有真的写库。修复 `086b85805` 的直接 `sum(actual_payable)` 会跳过 null 行，结果 = 0。
- 修正 `applyDerivedOnlyFromOrder`：
  - 优先用库列 `actual_payable`；
  - **该列为 null 时**，按 `应付金额 - 返点金额` 即时算（与 `fillPayableFields` 写库公式一致、也与前端详情页底部"各客户累加"展示一致）；不允许结果负值（<0 截 0）。
- 这次没改主表其他字段、不影响 create/update 路径（create/update 走 `applyIncomeFromPayables` 不变）。
- 提交：`8969ede8b`（fix(ad-order): 补数 actual_payable 为 null 时用应付-返点兜底）
- 验证：同一张订单 YYW-20260825-002 应出 21.80（10.90+10.90），应付返点 = 22.00 - 21.80 = 0.20，订单收入 = 21.56 - 21.80 = 负值截 0 后 = 0.00。

## 用户要求：负的就展示负的（不截 0）
- 用户明确："负的 就展示负的"。去掉 `applyDerivedOnlyFromOrder` fallback 里的负值截 0（原 `if (actual < 0) actual = 0` 删除）。
- 现在：实际应付如实累加（明细实际应付为负也保留），订单收入 = 应收 - 实际应付 可能为负，直接写入库、前端 `fmtAmount`（toLocaleString）正常负显。
- 前端 `fmtAmount`（frontend/packages/web/src/views/advertising/utils.ts）本身支持负显示，无需改前端。
- 提交：`10e7bf322`（fix(ad-order): 补数订单收入/实际应付允许负数直接展示）

## 关键根因：BaseMapper.select(E) 会把 int 默认 0 当 WHERE 条件
- 用户现场断点：`AdOrder criteria = new AdOrder(); criteria.setDeleted(0);` 之后，criteria 实例里 `int status/paymentDone/receiptDone/...` 都是默认 0，框架 `AbstractSqlProviderSupport.tableWhere` 用的判定是 `<if test="字段 != null">`，int 默认 0 不为 null → 拼进 WHERE。
- 实际生成的 SQL：
  ```sql
  SELECT ... FROM ad_order
  WHERE status = 0            -- 草稿
    AND receipt_done = 0
    AND payment_done = 0
    AND ...（所有 int 默认字段）
    AND deleted = 0
  ```
- 后果：**只有 status=0 的草稿订单被匹配，YYW-20260825-002（预付状态 → status>0）漏掉**。看起来"压根没获取到"。
- 修复：**走 XML 自定义 `selectAllNonDeleted`，只 WHERE deleted = 0**：
  - `ExtAdOrderMapper.java` 加 `List<AdOrder> selectAllNonDeleted();`
  - `ExtAdOrderMapper.xml` 显式列名（含三派生列）+ `WHERE deleted = 0`。
  - `AdOrderService.recomputeIncomeFieldsForAll` 改用 `extAdOrderMapper.selectAllNonDeleted()`。
- 同类坑警告：**项目里任何用 `BaseMapper.select(E criteria)` 做"全表扫描"都对 int 默认值敏感**——必须用 `selectAll(orderBy)` 或 XML 自定义，避免污染筛选条件。这是通用避坑点。
- 提交：`2b543ce66`（fix(ad-order): 补数改走 XML selectAllNonDeleted 绕开 select(E) int 误过滤）+ `a6818662`（日志 + `selectAllNonDeleted` 之前的对照日志）。验证步骤：重启后端 → admin 点「补算数据」→ 控制台应有 `[recompute-income] 候选订单数: N`（N 比之前大得多，含 status>0 订单），`affected=1`；库中抽查 YYW-20260825-002 实际应付=21.80、应付返点=0.20、订单收入=-0.24。

## 修复 Flyway V3.0.0.15 版本冲突（重要：删两处）
- 报错：`Found more than one migration with version 3.0.0.15`：
  - `ddl/V3.0.0_15__ad_resource_business_entity_nullable.sql`
  - `dml/V3.0.0_15__ad_permissions_v2.sql`
- 原因：Flyway 版本号全局唯一，不区分 ddl/dml 目录。提交 `0324caa65` 已把 ddl 那个从 git 删掉并改为 `V3.0.0_17`，但**磁盘上残留未跟踪文件**（git status 里是 untracked），且 `target/classes` 里还有编译拷贝 → 启动时 Flyway 扫 target 仍报冲突。
- **巨坑：`delete_file` 工具对已 tracked / 权限受限文件可能"静默成功但没真删"**。本次第一次删除后文件仍在，改用 `del /f /q` 命令行才真正删掉。**删除后必须 `list_dir` 复核。**
- 处理（两处都要删，只删 src 不够）：
  ```
  del /f /q backend\crm\src\main\resources\migration\3.0.0\ddl\V3.0.0_15__ad_resource_business_entity_nullable.sql
  del /f /q backend\crm\target\classes\migration\3.0.0\ddl\V3.0.0_15__ad_resource_business_entity_nullable.sql
  ```
  保留 `V3.0.0_17` 和 `dml/V3.0.0_15__ad_permissions_v2.sql`（后者是 8 月已应用的迁移，不能动）。
- 校验无重复版本号：ddl `1,4,5,7,8,10~14,16,17,21~28`；dml `1_1~1_4,2,3,6,9,15,18,19,20`，全局唯一。

## 工作台菜单改为「首页」
- 用户截图左侧菜单「工作台」需改为「首页」文案。
- 广告工作台（`/advertising/workbench`）已经是登录后默认首页：`getFirstRouteNameByPermission` 优先返回 `ADVERTISING_WORKBENCH`。
- 本次仅改菜单显示文案：
  - `frontend/packages/web/src/views/advertising/locale/zh-CN.ts`：`'module.advertising.workbench': '首页'`
  - `frontend/packages/web/src/views/advertising/locale/en-US.ts`：`'module.advertising.workbench': 'Home'`
- 路由图标原本就是 `iconicon_home`，无需再改。

## 修复 vue-tsc 编译报错（title 类型不匹配）
- 报错：`(column: TableColumnGroup<InternalRowData>) => VNodeChild` 不能赋给 `() => VNodeChild`（Target signature provides too few arguments）。
- 出现位置：`frontend/packages/web/src/components/pure/crm-table/type.ts` 第 36 行 `CrmDataTableColumn.title`。
- 根因：类型定义里手动把 `title` 覆盖成 `string | (() => VNodeChild)`，但 naive-ui 的 `TableColumnGroupTitle` 是 `string | ((column: TableColumnGroup) => VNodeChild)`（带一个 column 参数）。新增 `advertising.ts` 的 `AdOrderListItem`（含 `actualMediaPayableAmount`/`mediaRebateAmount`/`orderIncomeAmount`）用到分组列 `title: (column) => ...` 写法，类型对不上。
- 修复（**最终版，注意中间踩了两次坑**）：`title?: string | ((column: any) => VNodeChild);`
  1. 第一版改 `(() => VNodeChild)` → `((column: TableBaseColumn<T> | TableColumnGroup<T>) => VNodeChild)`，结果**反向报错**：`TableBaseColumn` 缺 `children`，不能赋给 `TableColumnGroup`。
  2. 根因：**函数参数是逆变检查**，联合类型下必须两个方向都兼容，写具体联合类型两头都不满足 → 只能把参数放宽为 `any`。
- 连带暴露并修掉的两个真实问题（原来被 title 报错掩盖）：
  1. `frontend/packages/lib-shared/models/advertising.ts` 的 `AdOrderListItem` 里 `actualMediaPayableAmount`/`mediaRebateAmount`/`orderIncomeAmount` **重复声明**（上面已有同样三个字段）→ 删掉重复的那组。
  2. `crm-table/type.ts` 的 `CrmDataTableColumn` 对 naive 类型的**收窄覆盖**导致外部 `DataTableColumn` 无法赋值：
     - `sorter?: boolean | 'default' | Sorter<T>`（原来只有 `boolean | 'default'`，缺自定义比较函数 `Sorter<T>`）
     - `filter?: boolean | ((optionValue: string | number, rowData: any) => boolean) | 'default'`（原来 `rowData: object`，`AdOrderListItem.id` 是必填字段，逆变检查下 `object` 不兼容 → 放宽为 `any`）
- 教训：`CrmDataTableColumn` 是 `TableBaseColumn | TableColumnGroup | ...` 的交叉/联合类型，**任何对 naive 类型的收窄覆盖都会在赋值时因逆变报错**，应保持与原类型一致或放宽为 `any`。
- 验证：`pnpm exec vue-tsc --noEmit` 全量通过（无输出）。
- 注：报错文件在用户临时目录 `C:\Users\cw\AppData\Local\Temp\codebuddy-dropped-files\...\advertising.ts`，但实际生效的是 `frontend/packages/lib-shared/models/advertising.ts`。

## 用印详情增加用印附件 + 合同编号超链接
- 需求：用印详情页（`advertising/seal/detail/:id`）缺「用印附件」；且「合同编号」要像审批中心那样可点击、新 tab 打开合同详情。
- **关键：用印附件不在 `ad_seal_record` 表上**。`AdSealRecord` 实体**没有任何附件字段**，用印上传（POST `/api/ad/seal/{contractId}/upload`）是把盖章版回填到**合同**的 `ad_contract.file_url`。所以附件要从合同上取。
- 后端改动：
  - `AdSealRecordDetailResponse.java`：新增 `contractId`、`contractFileUrl`
  - `AdSealRecordService.detail()`：在 `if (contract != null)` 里 `setContractId(contract.getId())` / `setContractFileUrl(contract.getFileUrl())`
- 前端改动：
  - `lib-shared/models/advertising.ts`：`AdSealRecordDetailResponse` 加 `contractId?`、`contractFileUrl?`
  - `views/advertising/seal/detail.vue`：合同编号改 `n-button text type="primary"` + `openContractDetail()`（用 `router.resolve` + `window.open(href,'_blank')`，与审批中心 `openLink` 一致）；新增「用印附件」descriptions-item，带预览/下载按钮。
- 附件预览/下载沿用合同详情写法：`/attachment/preview/${fileUrl}?userId=${userStore.userInfo?.id}`、`/attachment/download/...`，需要 `import useUserStore from '@/store/modules/user'`（import 排序在 `@/api/modules` 之后）。
- 验证：`vue-tsc --noEmit` 通过；`eslint --fix` 修掉 prettier 换行警告。

## 审批中心补充用印附件 + 归档审批合同编号超链接
- 文件：`frontend/packages/web/src/views/advertising/approval/index.vue`
- 该抽屉用统一的 `BaseField = { label, value, link?, linkId?, attachmentUrl? }` 驱动 `n-descriptions`，`openLink()` 走 `router.resolve` + `window.open(href,'_blank')`。
- 改动：
  1. `BaseField` 和 `detailBaseFields` ref 类型均加 `attachmentUrl?: string`（**两处都要改**，漏了 ref 声明会类型报错）。
  2. 模板 `n-descriptions-item` 内加 `v-if="f.attachmentUrl"` 分支渲染「预览/下载」按钮，原 link 分支改为 `v-else-if`。
  3. `type === 'seal'` 分支末尾加 `{ label: '用印附件', value: ..., attachmentUrl: res?.contractFileUrl }`（复用上一步后端新增的字段）。
  4. `type === 'archive'` 分支的「合同编号」原来没有 link，补上 `link: ADVERTISING_CONTRACT_DETAIL, linkId: c.id || id`。
- 附件预览/下载函数：`handlePreviewAttachment` / `handleDownloadAttachment`，同合同详情写法，需 `import useUserStore from '@/store/modules/user'`。
- 验证：`vue-tsc --noEmit` 通过，eslint --fix 后 0 error。
- 注意「用印附件」显示文本在有附件时是「查看附件」，实际渲染的是右侧预览/下载按钮。

## 收款单/付款单可选订单限定为 待执行/执行中/结算中
- 原状：两个页面的 `searchOrders()` 都直接 `getAdOrderPage({current:1,pageSize:20,keyword})`，**无任何状态过滤**，草稿/审批中/已归档/已作废的订单都能选。
- 状态码（后端 `OrderStateMachine` / 前端 `AdOrderStatusEnum` 一致）：草稿0 / 审批中10 / **待执行45** / **执行中50** / 改单审核中60 / **结算中80** / 已归档90 / 已作废100。
- **改动走后端过滤 + 前端兜底**（只做前端过滤会有隐患：pageSize=20 的第一页里若全是草稿/已归档，过滤后为空）：
  - `AdOrderPageRequest.java`：新增 `List<Integer> statusList`
  - `ExtAdOrderMapper.xml` 的 `pageList`：加 `statusList != null and size>0` 时 `o.status IN <foreach>`（模板同已有的 `selectTodoOrders`）
  - `AdOrderPageParams`（lib-shared）：加 `statusList?: number[]`
  - `receipt/index.vue` / `payout/index.vue`：`searchOrders` 传 `statusList: allowed`，并保留 `.filter()` 兜底；引入 `AdOrderStatusEnum`
- 验证：前端 `vue-tsc --noEmit` 通过、eslint --fix 0 error；后端 `mvn -pl crm -am compile` 通过。**需重启后端**才生效。
- 改单页 `change/create.vue` 用的是**排除法** `excluded=[0,10,60,90,100]`，语义等价（因为允许集恰好也是 45/50/80），但未同步改为白名单，后续若新增状态需留意。

## 审批中心列表：补「用印金额」 + 关联单号列头动态化
- 问题 1（用印申请没有金额）：`ExtAdApprovalMapper.xml` 的 `pendingSeals` SQL **没 SELECT `amount` 字段**。前端 `fmtAmount(row.amount)` 渲染时 amount 为 null → 显示 `-`（不是 0）。
  - 修复：SQL 补 `ct.amount AS amount`（LEFT JOIN 已经 join 了 ad_contract）。
- 问题 2（用印/归档列表头应是合同编号）：列表头 `key: 'refNo'` 写死成「关联单号」，但用印/归档这条 refNo 实际是合同编号。
  - 修复：i18n 增加 `refNo.contract` / `refNo.order` / `refNo.receipt` / `refNo.payout` 四种细分文案，列 `title` 改写成函数按 `currentType` 动态返回。
- 改动文件：`ExtAdApprovalMapper.xml`、`advertising/approval/index.vue`、locale `zh-CN.ts` / `en-US.ts`。
- 验证：`vue-tsc --noEmit`、`eslint --fix`、`mvn -pl crm -am compile` 均通过。

## 归档审批详情「合同金额 0.0」排查
- 现象：审批中心→归档审批 Tab→任一记录的详情抽屉里，「合同金额」字段显示 `0.00`。
- 排查：
  - `AdContract.amount` 是 `BigDecimal`；`detail()` 返回 `AdContractDetailResponse.setContract(c)`，**未丢失精度**（与 HashMap 无关）。
  - 前端 `c.amount != null ? fmtAmount(c.amount) : '-'`；`fmtAmount` 直接 `toLocaleString({minimumFractionDigits:2, ...})` 不会强制 0。
  - 结论：**`c.amount` 本身就是 0**，不是代码问题，是数据问题。
- 可能的来源（待排查）：
  1. 创建合同时前端 `n-input-number` 没填金额，被默认值/提交时 0 兜底
  2. 数据库 `ad_contract.amount` 字段迁移时被错误清零
  3. 归档审批 SQL `pendingArchives` 选了 `ct.amount`，列表头也显示金额，详情与列表一致时说明 DB 真为 0
- 处理思路：让用户 DBeaver/Navicat 查 `SELECT amount FROM ad_contract WHERE contract_no='CN20260901-9350'`；或临时在 `AdContractService.detail()` 加 `log.warn` 打金额，再读后端日志确认。

## 改单审批列表补金额（与用印同一类问题）
- 现象：审批中心→改单审批 Tab→「金额」列一直显示 `-`。
- 根因：`ExtAdApprovalMapper.xml` 的 `pendingChanges` SQL **没 SELECT amount**（LEFT JOIN 了 ad_order 但 SELECT 列表里没 `o.amount`）。
- 修复：补金额字段。**第一版写成了 `o.amount` 导致运行时报错 `Unknown column 'o.amount' in 'field list'`**（编译期发现不了，因为 XML 里的 SQL 不参与编译）。正确写法是 `o.total_amount AS amount`。
- **金额列名对照（重要，实测踩坑）**：
  | 表 | 金额列 |
  |---|---|
  | `ad_order` | **`total_amount`**（DDL `V3.0.0_1__ad_system_v31.sql` 第113行明确定义，全项目唯一） |
  | `ad_contract` | `amount` |
  | `ad_receipt` | `amount` |
  | `ad_payment` | `amount` |
- 6 个待审批 SQL 最终金额来源：
  - `pendingOrders` → `o.total_amount`
  - `pendingChanges` → `o.total_amount`（本条）
  - `pendingSeals` → `ct.amount`
  - `pendingArchives` → `ct.amount`
  - `pendingReceipts` → `r.amount`
  - `pendingPayouts` → `p.amount`
- **教训**：XML 里写的 SQL **编译期不校验列名**，改完 mapper XML 必须实际跑一次对应接口（或至少在 DB 里 `DESC 表名` 核对列名），不能只靠 `mvn compile` 通过就认为没问题。

## 订单审批详情补字段 + 投放周期格式化
- 详情抽屉已有 `金额/应收金额/应付`，缺 `实际应付 (actualMediaPayableAmount)` 和 `订单收入 (orderIncomeAmount)`，都已加进 `BaseField[]`。
- 投放周期之前是 `o.deliveryStartDate ~ o.deliveryEndDate` 字符串拼接，后端返回 `2026-08-31T16:00:00.000+00:00` 这种 ISO 字符串，直接拼太丑。改成 `${fmtDate(o.deliveryStartDate)} ~ ${fmtDate(o.deliveryEndDate)}`，`fmtDate` 来自广告模块的 `views/advertising/utils.ts`（已有，原 import 列表里只有 `fmtAmount`/`fmtDateTime`，补一个 `fmtDate`）。
- 其它审批 type（change/seal/receipt/payout/archive）都没有投放周期字段，只改了订单这一处。
- 验证：`vue-tsc --noEmit`、`eslint --fix` 均通过。

## 合同创建/编辑：移除关联订单必填 + 修复编辑回显
- 问题 1（必填校验太严）：`advertising/contract/create.vue` 的 `validate()` 对 `contractType=20`（单笔）要求关联订单必填，但合同与订单是多对多关系（`ad_order_contract` 中间表），合同应可先建、订单后建。**删除该校验块**。
- 问题 2（编辑回显为空）：`loadForEdit` 第 457 行从 `o.orderIds` 取关联订单 id，但 `AdContract` 实体**根本没有 `orderIds` 字段**（`AdContractSaveParams` 才有），结果 `undefined || []` → 下拉空白。
  - 修复：从详情接口的 `res.orderList`（`AdContractOrderVO[]`）提取 `orderId` 数组：`form.orderIds = (res.orderList || []).map(it => it.orderId).filter(Boolean)`。
  - 副作用：如果关联订单已被删除/不在 200 条列表里，`<n-select multiple>` 会显示空 label。在 `loadCommonOptions` 末尾补一个兜底：若回填的 id 不在已知 options 里，append 一个 `{label: oid, value: oid}` 占位选项。
- 踩坑：
  - `form.orderIds` 类型是 `string[] | undefined`，中间变量要先保底 `const formOrderIds = form.orderIds || []` 再访问 length。
  - 占位 option 用 `oid` 而非 `id`，避免与外层 `const id = route.params.id` 触发 `no-shadow` lint 报错。
- 验证：`vue-tsc --noEmit`、`eslint --fix` 均通过。

## 修复广告模块列表页无滚动条
- 现象：分页 10 条/页正常，改成 20 条/页后表格超出视口，没有滚动条，内容被截断。
- 根因：`layout/page-content.vue` 把路由视图包在 `h-full w-full overflow-hidden` 的绝对定位容器里；`n-scrollbar` 的 `content-style` 又写了 `height: 100%`，导致内容永远不会超过容器高度，滚动条无法出现。广告列表页 `.advertising-page` 只设了 padding，没有自己设 `height/overflow`，所以被截断。
- 修复：在全局样式 `frontend/packages/web/src/assets/style/global.less` 中给 `.advertising-page` 统一加：
  ```less
  .advertising-page {
    height: 100%;
    overflow-y: auto;
  }
  ```
  这样所有广告模块的 `.advertising-page` 页面都能自己滚动，且不影响工作台/详情页（它们已有相同作用的 scoped 样式，优先级更高）。
- 注意：这只是让页面整体滚动。如果想让表格表头固定、只滚动 tbody，需要把 `.advertising-page` 改成 flex 列布局，并给 `n-data-table` 加 `flex-height`，那是另一个改造量。

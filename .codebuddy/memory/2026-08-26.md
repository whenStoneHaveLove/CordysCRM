# 2026-08-26

## 订单新建界面区块顺序调整（已提交 cdc9a28d9）
- 把「金额与返点(5)」「投放信息(6)」移到「合作方(2)」下方，变为 3/4；原「收款方式(3)」「付款方式(4)」顺延为 5/6；附件与合同 7 不变。
- 仅改 `frontend/packages/web/src/views/advertising/order/create.vue` 模板顺序与序列号，逻辑/校验未动。

## 订单新建「付款方式」区块：新增每下游客户付款返点明细（已实现，未提交）
- 需求：下游客户多选，每选一个展示该客户的付款返点卡片（应付金额 / 不记返金额 / 返点方式 / 返点比例 / 返点金额(自动) / 实际应付）。
- 明确与订单级返点（ad_order 的 media_payable_amount/rebate_*）**完全独立，不改动旧逻辑**。
- 方案（用户确认"按建议来"）：字段加在 `ad_order_downstream_media` 关联表上（不新建表、不用 JSON）。
- 改动文件：
  - DDL: `V3.0.0_22__ad_order_downstream_media_payable.sql` 给 `ad_order_downstream_media` ALTER 加 6 列：`payable_amount`/`no_rebate_amount`/`rebate_mode`/`rebate_value`/`rebate_amount`/`actual_payable`。
  - Entity `AdOrderDownstreamMedia`: 加 6 个 BigDecimal/Integer 字段。
  - `AdOrderSaveRequest`: 加 `List<DownstreamMediaPayableDTO>`（含 downstreamMediaId + 4 输入字段）。
  - `AdOrderService`: `syncOrderDownstreamMedia` 改为接收 payables，复用旧行(setDeleted0)或新建时都写入付款字段；`fillPayableFields` 自动算 `rebateAmount=(payable-noRebate)*rebateValue/100`(比例) 或 `rebateValue`(固定)，`actualPayable=payable-rebateAmount`。`getDetail` 回填 `downstreamMediaPayables` VO（含客户名称，从 `ExtAdDownstreamMediaMapper.selectByPrimaryKey` 取）。
  - `AdOrderDetailResponse`: 加 `List<DownstreamMediaPayableVO>`（含 downstreamMediaName）。
  - `create.vue`: form 加 `downstreamMediaPayables[]`；watch downstreamMediaIds 同步卡片；付款区块加每客户卡片 + `payableAutoCalc`；`buildPayload` 带明细；`loadForEdit` 回填；i18n 加 `payableAmount`/`actualPayable`（zh-CN/en-US）。
  - `detail.vue`: 只读展示每客户付款返点明细卡片。
- 公式验证：22-4=18, 18*2%=0.36, 22-0.36=21.64 ✓
- 注意：未提交。后端需 Maven 3.6.3+ 重新 package 让 DDL 生效；lint 全为历史遗留/误报，无新增错误。

## 付款单（ad_payment）：各下游客户付款返点明细（已实现，未提交）
- 需求：①新建付款单展示订单各客户的返点信息(应付/不记返/返点方式/返点值/返点金额/实际应付)+本次付款输入；②付款单详情展示各客户付款明细表；③审核页(payout tab)详情抽屉嵌入付款单详情(基础字段 + 各客户付款明细表)。
- 方案：新建 `ad_payment_media` 明细表（每客户一行，冗余 media 名称与返点字段）+ 新 DDL `V3.0.0_23__ad_payment_media.sql`。
- 后端改动：
  - `AdPaymentMedia`(domain) / `AdPaymentMediaMapper`(extends BaseMapper) / `ExtAdPaymentMediaMapper`(selectByPaymentId/deleteByPaymentId/selectEntitiesByPaymentId)。
  - `AdPayoutMediaOptionResponse` 增加返点 6 字段 + paidAmount(累计已付，子查询 JOIN ad_payment_media SUM 含 status 0/10/20)。
  - `ExtAdPayoutMediaOptionMapper.selectMediaOptions` SQL JOIN ad_downstream_media 填充名称 + 返点字段 + 累计已付。
  - `AdPayoutSaveRequest` 加 `List<AdPayoutMediaDetail>`；`AdPayoutMediaDetail`(request DTO)。
  - `AdPayoutDetailResponse` 加 `List<AdPayoutMediaDetailItem>`；`AdPayoutMediaDetailItem`(response DTO)。
  - `AdPayoutService`：`saveMediaDetails(p, details, userId, orgId, now)` 先清后插；create/update 调用；detail 加载 `mediaDetails`。
- 前端改动：
  - `lib-shared/models/advertising.ts`：`AdPayoutSaveParams` 加 `mediaDetails`；新增 `AdPayoutMediaDetailItem`/`AdPayoutMediaOption`；`AdPayoutDetail`/`AdPayoutInfo` 加 `mediaDetails`。
  - `lib-shared/api/modules/adPayout.ts`：`getAdPayoutMedia` 返回类型改为 `AdPayoutMediaOption[]`。
  - `payout/index.vue`：新建表单选订单后展示「各下游客户返点与本次付款」卡片(圆圈序号 + 左 border 主色)，每客户本次付款输入自动汇总到 form.amount；详情弹窗加「各下游客户付款返点明细」表；modal 宽 880/760。
  - `approval/index.vue`：detailType==='payout' 时详情抽屉加「各下游客户付款返点明细」表（来自 res.mediaDetails）。
- 校验：后端 `mvn compile`(Maven 3.9.4 + JDK21) BUILD SUCCESS；前端 ESLint 0 错误；vue-tsc 无 payout/approval/advertising 报错。
- 注意：未提交。Maven 必须用 `D:\apache-maven-3.9.4\bin\mvn` + `JAVA_HOME=D:\Java\jdk21`（系统默认 3.3.3/1.8 编译不过）。

## 下游客户银行账户 + 付款单/收款单账户下拉与提交按钮（已提交 e35914a4b + 清理 007f57497）
- 需求（用户图片）：①下游客户加「银行账户」区块（收款人全称/开户行/银行账号/是否停用），可 `+` 增多个，逻辑删除不可真删，不挂独立权限（沿用下游客户 AD_DOWNSTREAM_MEDIA 权限）；②付款单每个客户卡片加「账户信息」下拉（展示 收款人全称-开户行-银行账号），默认回带最早一条可用账户（disabled=0 且 deleted=0），提交必校验账户、保存放过；付款单明细(ad_payment_media)记录 account_id；③收款/付款 新建编辑界面 保存后增加「提交」按钮（草稿status=0 → 待审核status=10，沿用既有 submit 接口）。
- 方案：
  - DDL `V3.0.0_24__ad_downstream_media_account.sql`：新表 `ad_downstream_media_account`(id/downstream_media_id/payee_name/bank_name/bank_account/disabled/organization_id/审计/deleted)；并 `ALTER ad_payment_media ADD account_id`。
  - 后端：`AdDownstreamMediaAccount`(extends BaseModel)；`ExtAdDownstreamMediaAccountMapper`(selectByDownstreamMediaId/selectFirstAvailable/selectByDownstreamMediaIds + XML)；`AdDownstreamMediaAccountItem`/`AdDownstreamMediaAccountSaveItem`(DTO)；`AdDownstreamMediaSaveRequest.accounts`；`AdDownstreamMediaDetailResponse.accountList`；`AdDownstreamMediaService.saveAccounts`(先清后插/逻辑删不在列表内的)；新增 controller `GET /{id}/accounts`。
  - 付款单：`AdPaymentMedia.accountId`；`AdPayoutMediaDetail.accountId`(req)；`AdPayoutMediaDetailItem` 加 accountId/payeeName/bankName/bankAccount/accountDisabled；`ExtAdPaymentMediaMapper.selectByPaymentId` LEFT JOIN 账户表带出显示字段；`AdPayoutMediaOptionResponse.accountList`（付款单选订单时按 mediaIds 批量拉账户）；`AdPayoutService.listMedia` 填充 accountList。
  - 前端：`AdDownstreamMediaAccount`/`AdDownstreamMediaAccountSaveItem` 类型；`adDownstreamMedia` API 加 `getAdDownstreamMediaAccounts` + detail 返回类型；`downstreamMedia/index.vue` 加账户卡片区 `+`/删除 + 保存/编辑回填（编辑需先 getDetail 才带 accountList）。
  - 付款单 `payout/index.vue`：每个客户卡片加账户 n-select(`accountOptionsOf` 用 media.accountList)，`mediaAccountDraft` 草稿；新建模式默认 `firstAvailableAccountId`；提交 `handleSubmitModal` 校验所有客户都已选账户→保存→submitAdPayout；详情表加账户列。
  - 收款单 `receipt/index.vue`：同样加「提交」按钮（`handleSubmitModal` → 保存→submitAdReceipt）。
  - 审核 `approval/index.vue`：付款明细表加账户列(`formatAccountCell`)。
- 校验：后端 mvn compile SUCCESS；前端 vue-tsc EXIT=0、ESLint 0 error（仅 `no-undef` 警告，非错误）。
- 提交排除：`build_compile.bat`/`tsc_check.bat`/`compile_out.txt`/`tsc_out.txt` 为临时文件，勿提交（已用 git rm --cached 清理）。
- 注意：`AdPayoutService.saveMediaDetails` 用逐条 insert 而非 `batchInsert`（BaseMapper.batchInsert 列表参数绑定有坑，见前面记录）。
## ���οͻ��༭�����޸������ύ 28874ab81�� 
-- ��� AdDownstreamMediaDetailResponse ��Ƕ�׽ṹ������ֻ�� media + ���� *Label + accountList��������ƽ�̡� 
-- ǰ�� openEdit ����ش� d.name/d.mediaType �� �� ȫ�� undefined���޷����ԣ��˻��ܻ�������Ϊ��˵��� setAccountList���� 


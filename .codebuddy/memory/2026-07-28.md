# 2026-07-28

## 修复 MyBatis BindingException
- 修复了 `ExtAdDownstreamMediaMapper.pageList` 和 `ExtAdUpstreamAgentMapper.pageList` 的 BindingException
- 原因：两个 Mapper XML 文件被错误放置在 `src/main/resources/mapper/ad/` 下，未配置 `mybatis.mapper-locations` 导致找不到
- 解决：将 XML 文件移动到 Java 接口同目录（与 ExtAdReportMapper.xml 保持一致）

## 修复上游代理新建弹窗缺少保存/取消按钮
- `upstreamAgent/index.vue` 的 `n-modal` 使用了 `preset="card"`，但 `positive-text`/`@positive-click` 等 prop/event 在 `card` 模式下不生效
- 改用 `<template #footer>` 插槽手动渲染按钮
- 同样问题页面：downstreamMedia、payment/*、order/detail、system/business-entity
